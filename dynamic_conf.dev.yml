http:
  routers:
    dev-message:
      rule: "PathPrefix(`/health-test`)"
      entryPoints:
        - "http"
      service: "dev-message"

  services:
    dev-message:
      loadBalancer:
        servers:
          - url: "http://traefik-dev-message:5678"
